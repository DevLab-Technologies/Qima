source "https://rubygems.org"

gem "fastlane"

# Commit the Gemfile.lock generated by the first `bundle install` (local or CI)
# so later runs resolve the same gem versions - same convention as ios/Gemfile.
