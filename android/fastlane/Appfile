package_name("com.devlabtechnologies.qima")

# No json_key_file here on purpose: the Google Play service account key is read
# from the PLAY_SERVICE_ACCOUNT_JSON environment variable (see Fastfile), so it
# never has to exist on disk or in the repository.
