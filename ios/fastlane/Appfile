app_identifier("com.devlabtechnologies.qima")
team_id("44PL6FHCG3") # "Miza Capital Company" (Company account). Runner.xcodeproj uses ZS3A435WC2 - keep the two in step.

# No apple_id here on purpose: this project authenticates to App Store Connect
# with an API key (see app_store_connect_api_key in Fastfile), not an Apple ID.
# Apple ID + password sessions expire under 2FA and are not usable in CI.
